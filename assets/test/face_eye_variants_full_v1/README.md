# Full face eye variant comparison

Each set contains 28 frames for both male and female bases: standing and walking frames in all four directions. The front standing frame uses stable mirrored eye geometry; side, back, and motion frames preserve their original visible-pixel layout and apply the same scale factors. Back-facing frames keep transparent eye layers. Generation is deterministic CPU-only Pillow processing; no GPU or model runtime is required.
